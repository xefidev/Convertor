{
  "name": "flux-convert",
  "version": "1.0.0",
  "private": true,
  "description": "Premium currency and crypto converter landing page",
  "scripts": {
    "build": "node build.js",
    "start": "python -m http.server 3000"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
